version: '3'

#dotenv: [ '.env' ]

tasks:
  clean:
    cmds:
      - rm -r backend/target
      - rm -r client/node_modules
  serve:
    dir: ./client
    cmds:
      - npm start
  down:
    cmds:
      - docker-compose down
